"use client";
import {FormEvent,useState}from"react";import{getSession,signIn}from"next-auth/react";import{useRouter}from"next/navigation";
export function LoginForm(){const router=useRouter();const[error,setError]=useState<string|null>(null);const[pending,setPending]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError(null);setPending(true);const data=new FormData(event.currentTarget);const result=await signIn("credentials",{identifier:String(data.get("identifier")??""),password:String(data.get("password")??""),redirect:false});if(result?.error){setError("Identifiants incorrects.");setPending(false);return}const session=await getSession();router.replace(session?.user.role==="ADMIN"?"/admin":"/client");router.refresh()}return <form className="mt-8 space-y-5" onSubmit={submit}><label className="block text-sm font-medium">Adresse email ou téléphone<input required name="identifier" autoComplete="username" className="mt-2 w-full rounded-xl border border-white/15 bg-graphite px-4 py-3 text-white outline-none focus:border-electric-mint"/><small className="mt-2 block text-muted">Utilisez votre adresse email. Les anciens comptes peuvent encore utiliser leur numéro de téléphone.</small></label><label className="block text-sm font-medium">Mot de passe<input required name="password" type="password" autoComplete="current-password" className="mt-2 w-full rounded-xl border border-white/15 bg-graphite px-4 py-3 text-white outline-none focus:border-electric-mint"/></label>{error&&<p role="alert" className="rounded-lg border border-red-400/30 bg-red-400/10 px-3 py-2 text-sm text-red-200">{error}</p>}<button disabled={pending} className="w-full rounded-xl bg-electric-mint px-4 py-3 font-semibold text-graphite disabled:opacity-60">{pending?"Connexion…":"Se connecter"}</button></form>}
