"use client";
import { useActionState } from "react";
import type { ProjectType } from "@prisma/client";
import { convertOrderToProject } from "@/features/order-conversion/action";
import { initialConvertOrderActionState } from "@/features/order-conversion/types";
import { projectTypeLabels } from "@/features/projects/labels";
export function ConvertOrderForm({orderId,name,description,projectType}:{orderId:string;name:string;description:string;projectType:ProjectType}){const boundAction=convertOrderToProject.bind(null,orderId);const[state,action,pending]=useActionState(boundAction,initialConvertOrderActionState);return <form action={action} className="grid gap-4 md:grid-cols-2" onSubmit={event=>{if(!window.confirm("Cette commande sera transformée en projet de production. Continuer ?"))event.preventDefault()}}><label className="text-sm font-medium">Nom du projet *<input required minLength={2} maxLength={160} name="projectName" defaultValue={name} className="mt-2 w-full rounded-xl border border-white/15 bg-graphite px-4 py-3"/></label><label className="text-sm font-medium">Type de projet *<select required name="projectType" defaultValue={projectType} className="mt-2 w-full rounded-xl border border-white/15 bg-graphite px-4 py-3">{Object.entries(projectTypeLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="text-sm font-medium md:col-span-2">Description<textarea maxLength={2000} rows={5} name="projectDescription" defaultValue={description} className="mt-2 w-full rounded-xl border border-white/15 bg-graphite px-4 py-3"/></label>{state.status==="error"&&<p role="alert" className="text-sm text-red-300 md:col-span-2">{state.message}</p>}<button disabled={pending} className="w-fit rounded-xl bg-electric-mint px-5 py-3 font-semibold text-graphite disabled:opacity-60">{pending?"Création…":"Créer le projet"}</button></form>}
