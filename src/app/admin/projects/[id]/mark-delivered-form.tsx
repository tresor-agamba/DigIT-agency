"use client";import{useActionState}from"react";import{markProjectDeliveredAction}from"@/features/final-deliverables/actions";import{initialActionState}from"@/features/versions/types";
export function MarkDeliveredForm({projectId}:{projectId:string}){const[s,a,p]=useActionState(markProjectDeliveredAction,initialActionState);return <form action={a} className="mt-4"><input type="hidden" name="projectId" value={projectId}/>{s.status!=="idle"&&<p className={s.status==="success"?"text-electric-mint":"text-red-300"}>{s.message}</p>}<button disabled={p} className="mt-3 rounded-lg bg-electric-mint px-4 py-2 font-semibold text-graphite">{p?"Validation…":"Marquer comme livré"}</button></form>}
