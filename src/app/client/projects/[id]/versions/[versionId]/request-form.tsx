"use client";
import { useActionState } from "react";
import { createModificationRequest } from "@/features/modification-requests/actions";
import { initialRequestActionState } from "@/features/modification-requests/types";
export function RequestForm({projectId,versionId}:{projectId:string;versionId:string}) { const [state,action,pending]=useActionState(createModificationRequest,initialRequestActionState); const field="rounded-lg border border-slate-300 bg-white px-3 py-2 text-slate-950 outline-none focus:border-teal-500 focus:ring-2 focus:ring-teal-100"; return <form action={action} className="mt-4 grid gap-3 rounded-xl border border-slate-200 bg-slate-50 p-4"><input type="hidden" name="projectId" value={projectId}/><input type="hidden" name="versionId" value={versionId}/><label className="grid gap-1 text-sm font-semibold text-slate-700">Titre<input required name="title" placeholder="Titre de votre demande" className={field}/></label><label className="grid gap-1 text-sm font-semibold text-slate-700">Description<textarea required name="description" minLength={10} rows={5} placeholder="Décrivez précisément la modification souhaitée" className={field}/></label>{state.status!=="idle"&&<p role="status" className={state.status==="success"?"text-teal-700":"text-red-700"}>{state.message}</p>}<button disabled={pending} className="w-fit rounded-lg bg-electric-mint px-4 py-2 font-bold text-slate-950 disabled:opacity-60">{pending?"Envoi…":"Envoyer la demande"}</button></form>; }
