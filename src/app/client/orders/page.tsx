import Link from"next/link";import{requireClient}from"@/lib/auth/guards";import{findClientOrders}from"@/features/orders/queries";import{StatusBadge}from"@/components/client/status-badge";import{EmptyState}from"@/components/client/empty-state";
export default async function ClientOrdersPage(){const client=await requireClient(),orders=await findClientOrders(client.id);return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><p className="text-xs font-bold tracking-[.18em] text-teal-600">COMMANDES</p><h1 className="mt-3 text-3xl font-black text-slate-950">Mes commandes</h1><p className="mt-2 text-slate-600">Suivez les demandes transmises à DigIT Agency.</p>{orders.length?<section className="mt-8 grid gap-4">{orders.map(order=><Link key={order.id} href={`/client/orders/${order.id}`} className="grid min-w-0 gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,.03)] transition hover:border-teal-400 sm:grid-cols-[1fr_auto] sm:items-center"><div className="min-w-0"><p className="text-xs font-bold text-teal-600">{order.orderNumber}</p><h2 className="mt-2 truncate text-lg font-bold text-slate-950">{order.title}</h2><p className="mt-1 text-sm text-slate-500">{order.service.name} · {order.createdAt.toLocaleDateString("fr-FR")}</p></div><div className="flex items-center gap-4"><StatusBadge kind="order" status={order.status}/><span className="font-bold text-teal-600">→</span></div></Link>)}</section>:<div className="mt-8"><EmptyState title="Vous n’avez pas encore de commande." text="Présentez-nous votre besoin pour démarrer votre collaboration avec DigIT Agency." action={{href:"/order",label:"Démarrer un projet"}}/></div>}</main>}
