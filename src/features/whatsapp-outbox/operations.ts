import{prisma}from"@/lib/prisma";export class WhatsAppOutboxError extends Error{}export async function retryFailedWhatsApp(actorId:string,id:string){const actor=await prisma.user.findUnique({where:{id:actorId},select:{role:true}});if(actor?.role!=="ADMIN")throw new WhatsAppOutboxError("Action réservée aux administrateurs.");const result=await prisma.whatsAppOutbox.updateMany({where:{id,status:"FAILED",providerStatus:null},data:{status:"PENDING",nextAttemptAt:new Date(),failedAt:null,lastError:null,processingStartedAt:null}});if(result.count!==1)throw new WhatsAppOutboxError("Message WhatsApp non éligible au retry.");return{id}}
