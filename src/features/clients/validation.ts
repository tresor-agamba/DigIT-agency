import{z}from"zod";import{phoneSchema}from"@/lib/validation/phone";import{optionalEmailSchema}from"@/lib/validation/email";export const clientIdSchema=z.string().cuid();export const createClientSchema=z.object({name:z.string().trim().min(2).max(120),email:optionalEmailSchema,phone:phoneSchema,temporaryPassword:z.string().min(12).max(128)});export const updateClientSchema=z.object({id:clientIdSchema,name:z.string().trim().min(2).max(120),email:optionalEmailSchema,phone:phoneSchema,isActive:z.boolean()});
