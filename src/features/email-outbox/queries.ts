import type{EmailOutboxStatus,Prisma}from"@prisma/client";import{prisma}from"@/lib/prisma";
export function getEmailOutbox(input:{status?:EmailOutboxStatus;query?:string;page?:number}){const page=Math.max(1,input.page??1),q=input.query?.trim();const where:Prisma.EmailOutboxWhereInput={...(input.status?{status:input.status}:{}),...(q?{OR:[{toEmail:{contains:q,mode:"insensitive"}},{type:{equals:q.toUpperCase() as never}}]}:{})};return prisma.emailOutbox.findMany({where,select:{id:true,status:true,type:true,toEmail:true,attempts:true,maxAttempts:true,nextAttemptAt:true,providerMessageId:true,createdAt:true,sentAt:true,lastError:true},orderBy:{createdAt:"desc"},skip:(page-1)*25,take:25})}
