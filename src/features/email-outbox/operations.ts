import{prisma}from"@/lib/prisma";export class EmailOutboxError extends Error{}
export async function retryFailedEmailAs(actorId:string,id:string){const actor=await prisma.user.findUnique({where:{id:actorId},select:{role:true}});if(actor?.role!=="ADMIN")throw new EmailOutboxError("Action réservée aux administrateurs.");const result=await prisma.emailOutbox.updateMany({where:{id,status:"FAILED"},data:{status:"PENDING",nextAttemptAt:new Date(),failedAt:null,lastError:null,processingStartedAt:null}});if(result.count!==1)throw new EmailOutboxError("Email en échec introuvable.");return{id}}
