import{Readable}from"node:stream";import{privateFileInfo,privateFileStream}from"@/lib/storage/private-storage";import{sanitizeFinalFileName}from"./validation";
export async function createFinalDownloadResponse(key:string,mime:string,name:string){const{absolutePath,size}=await privateFileInfo(key);const stream=Readable.toWeb(privateFileStream(absolutePath)) as ReadableStream;const safe=sanitizeFinalFileName(name);return new Response(stream,{headers:{"Content-Type":mime,"Content-Length":String(size),"Content-Disposition":`attachment; filename="${safe.replace(/[^\x20-\x7e]/g,"_")}"; filename*=UTF-8''${encodeURIComponent(safe)}`,"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}
